# Base Python version
python==3.10

# PyTorch and related packages
# Note: PyTorch packages are typically installed using specific index URLs.
# For CUDA 12.4, install using:
pip install torch==2.5.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# PyTorch Geometric and associated libraries
# These packages often require installation via specific wheels.
# For PyTorch 2.5.0 with CUDA 12.4, install using:
pip install pyg_lib torch_scatter torch_sparse torch_cluster torch_spline_conv -f https://data.pyg.org/whl/torch-2.5.0+cu124.html
# Then install torch_geometric:
pip install torch_geometric

# RDKit and related packages
# RDKit is typically installed via conda due to its complex dependencies.
# Install using:
conda install -c conda-forge rdkit openbabel fsspec

# Additional Python packages
pytorch-lightning
yacs
torchmetrics
performer-pytorch
tensorboardX
ogb
wandb
pymetis
